export type Author = {
    code: string,
    first_name: string,
    last_name: string
}

export type SimpleListItem = {
    name: string
}

export type ExpAuthor = {
    id: number,
    author_code: string,
    exp_num: string,
    first_name: string,
    last_name: string
}